[package]
name = "libafix"
version = "1.0.0"
edition = "2021"
description = "Core library for reading and writing .aFix image files"
license = "MIT"

[dependencies]
byteorder = "1"
crc32fast = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
